import { ToptroisFilterPipe } from './toptrois-filter.pipe';

describe('ToptroisFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ToptroisFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
